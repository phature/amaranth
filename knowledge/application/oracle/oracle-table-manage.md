# Oracle表管理

## 空表分配空间

``` text
SELECT 'ALTER TABLE ' || A.TABLE_NAME || ' ALLOCATE EXTENT(SIZE 64K);'
  FROM USER_TABLES A
  LEFT JOIN USER_SEGMENTS B
    ON A.TABLE_NAME = B.SEGMENT_NAME
   AND UPPER(B.SEGMENT_TYPE) = UPPER('TABLE')
 WHERE B.SEGMENT_NAME IS NULL;
```
